namespace ItemChecklist.UI
{
    /// <summary>The four switchable list sort modes (Iter-10). Found and
    /// Category were removed — Found is redundant with the discovery filter and
    /// Category has no total order (it became a filter).</summary>
    public enum SortMode { Name, Rarity, Level, Value }
}
